import time
import random

def search_books(consulta):
    time.sleep(random.randint(a=2, b=60)) # NUmeros randomicos
    return [f"Resultado não encontrado para '{consulta}'"]
    # return [f"📗 Resultado {i+1} para '{consulta}'" for i in range(5)]